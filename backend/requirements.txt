# What this app actually imports. The previous file was a pip freeze of an
# unrelated environment - torch, fairseq, librosa, rvc-python and eighty other
# packages - which turned a fresh install into a multi-gigabyte download that
# fails on any machine without a CUDA toolchain.
anthropic==1.4.0
Flask==3.1.0
flask-cors==5.0.1
fpdf==1.7.2
python-dotenv==1.0.1
python-pptx==1.0.2
